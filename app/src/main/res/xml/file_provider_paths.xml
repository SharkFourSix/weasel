<?xml version="1.0" encoding="utf-8"?>
<paths
    xmlns:android="http://schemas.android.com/apk/res/android">
    <external-cache-path
        name="shared-external"
        path=".shared/"/>

    <!--
    <external-path
        name="downloads"
        path="." /> -->

    <!--
        The hack below allows us to share songs from the root because the above doesn't work.

        The hack is kind of evil though :/
     -->
    <!--suppress AndroidElementNotAllowed -->
    <root-path
        name="root"
        path="."/>
</paths>